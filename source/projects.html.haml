---
title: Projects
---

.home
    .top.top_one
        .top_two
    .middle
        %p projects
    .bottom
        .bottom_one
        .bottom_two

// = partial 'partials/projects'

.projects
    - data.projects.each do |project|
        .project_box
            .project_info
                %h4= project[:name]
            .project_link
                %button go to project